# Task Tree

- [done] 修复 OpenAI 429 自动重试
  - [done] 确认请求重试与不可恢复错误 hook 的边界
  - [done] 调整 429 分类并补充回归测试
  - [done] 运行相关检查并记录结果

# Details

- 用户要求：OpenAI 429 不应直接作为不可恢复错误交由 hook 处理，应直接重试。
- 仅修改相关请求错误处理；保留工作区已有的无关修改。
- JVM loopback 基线确认 HTTP 层已重试 429；耗尽后 SSE catch 仅识别 I/O 异常，导致 429 直接穿透而非交回 Agent 重试。
- 用真实客户端与 loopback HTTP 服务验证 429 后成功、持续 429 耗尽、禁用重试和其他 4xx；复用现有状态码分类、退避及次数上限，不改变 hook 最终失败边界。
- 验证顺序：新增 HTTP 回归测试并确认旧实现失败，再修正异常重试分类，运行 OpenAI client 与 SSE 工具模块检查。
- 修复点为 HTTP 异常重试回调及 Responses 流的 catch：复用已有 HTTP/传输异常分类，让包装为 SSE 异常的 429 先按 HTTP 策略重试，耗尽后按未完成流进入 Agent 既有有限重试；不增加重试状态或改变次数配置。
- 修改前新增的“HTTP 重试耗尽后交回 Agent”与“下一次请求恢复”两个 JVM loopback 用例均以 `SSEClientException` 失败，确认回归覆盖缺陷。
- 实现：`Kodex/openai/client/src/commonMain/kotlin/io/github/stream29/kodex/openai/client/OpenAiClient.kt:99`、`:342` 复用同一异常分类；HTTP 重试与 Agent 重试均耗尽后仍保留最终失败报告。
- 验证：OpenAI client JVM 72、Linux X64 62，SSE 工具 JVM 11，AgentState JVM 34，compaction runtime JVM 11 个测试全部通过；后两组已强制重跑。
- 真实 I/O：新增 9 个 JVM loopback 用例覆盖两种 Responses 调用、429 重试与后续恢复、禁用开关、400/401/403/404；Native 运行共享单元测试，未进行真实 HTTP 或完整 CLI 验证。
- JVM 与 Linux X64 编译通过；IDEA 修改文件错误检查、`git diff --check` 通过。未调用真实 OpenAI 服务或构建发布包；未创建 Git 提交。
- 启动时无运行中的 Gradle Daemon，显式使用最近 Daemon 的 Java 25 路径启动，后续检查复用该 Daemon。
