# Task Tree

- [done] 将状态栏的模式摘要改为模式选择器
  - [done] 移除独立显示的`default`/`plan`摘要和`[Plan]`动作按钮
  - [done] 提供`[default mode]`或`[plan mode]`单一入口及上拉菜单
  - [done] 覆盖鼠标、焦点、方向键、Enter和Esc交互测试

# Details

模式是会话设置，状态栏只保留一个可操作入口。选择器显示当前模式并提供两种模式的明确选择。
