# Task Tree

- [done] 停止 SQLDelight/SQLite session storage 规划

# Details

- 用户决定不采用 SQLite。
- 先前讨论的 `sessions.db`、SQLDelight、SQLite schema、driver 和
  session-storage redesign 均不进入实现。
- 后续工作改为在文件存储基础上优化。
