# Task Tree

- [done] 分批提交当前混合改动
  - [done] 盘点内外仓库改动及任务归属
  - [done] 确定提交批次、依赖和校验方式
  - [done] 逐批暂存、检查并创建提交
    - [done] OpenAI 429 重试及配套文档
    - [done] unified-exec yield 及配套文档
    - [done] 菜单时间戳、窄视口修复及配套文档
    - [done] Revert and edit 及配套文档
    - [done] kRPC 讨论快照
  - [done] 核对提交历史及剩余工作区

# Details

- 用户已明确授权将本地混合改动分批提交；不推送远端，不扩展实现范围。
- 保留工作区原始内容；同文件不同任务按代码块拆分，避免整文件混入无关修改。
- 同时核对 `Kodex/` 子仓库和外层工作流文档；提交前检查暂存差异和新增文件。
- 路线：四组代码批次依次为 OpenAI 429、unified-exec yield、菜单时间戳及同任务窄视口修复、Revert and edit。
  每组同步单独提交外层相关文档和对应子模块指针；kRPC 讨论文档独立提交且不推进其任务。
- 重叠文件通过暂存区的中间版本拆分，不改写工作区；最后核对所有原始工作区内容均被保留。
- 复用已有验收记录，提交前重跑相关 JVM 测试；分批中间树做暂存差异和调用依赖检查，不宣称逐中间树运行过测试。
- 已提交：OpenAI `8dcb12ea` / 外层 `046b810`；unified-exec `8e5d9a6e` / 外层 `e861312`。
- 已提交：时间戳及布局 `2f469fac` / 外层 `25da560`；Revert and edit `a5d40e33` / 外层 `157dbea`。
- kRPC 讨论快照：外层 `9e5fac1`，原文不变，仍保持 Discussion。
- 十模块 JVM 强制重跑中，`app-viewmodel-agent` 的稀疏历史/revert 用例一次超时；未修改源码，单独强制重跑该模块通过。
  其余九模块通过；保留这次失败记录，不把整体首跑描述为全绿。
- 最新十模块测试报告共 353 项通过、0 skipped；本次未重做 Native 构建或实际 CLI 验收，沿用各实现任务的记录。
- 已用内容哈希逐文件确认：原有改动及新增文件与最终 HEAD 完全一致；工作区未因拆分而改写，暂存区中间版本仅用于独立提交。
- 四个外层配套提交依次指向对应代码提交，最终 `Kodex` gitlink 与子仓库 HEAD `a5d40e33` 一致。
- 收尾前子仓库已干净，外层只剩本任务记录；临时拆分脚本、校验快照与测试日志在记录提交前清理。不推送远端。
