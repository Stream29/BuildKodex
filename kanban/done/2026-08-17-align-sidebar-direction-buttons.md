# Task Tree

- [done] 统一侧栏展开与收起按钮
  - [done] 记录方向按钮视觉决策
  - [done] 将收起态替换为 `[→]`
  - [done] 保持展开态 `[←]` 与宽度动画
  - [done] 更新侧栏渲染测试
  - [done] 更新 TUI 交互决策
  - [done] 运行格式化和回归验证

# Details

- 状态：`done`。
- 用户已明确要求直接落地。
- 当前展开态已经使用 `[←]`；收起态将由三行圆角书签改为同一按钮样式的单行 `[→]`。
- 不改变侧栏的悬停临时展开、点击固定展开或宽度动画。
- IDEA 格式化、定向构建与检查通过。
- `:app-view-application:linuxX64Test` 通过；侧栏测试共 5 项，失败与错误均为 0。

## 1. 侧栏方向按钮

- 收起态和展开态使用相同的方括号按钮视觉，只通过箭头方向表达操作结果。

### 用户审批

- 不使用圆角书签；改为 `[<方向键>]`，使展开和收起保持一致。
