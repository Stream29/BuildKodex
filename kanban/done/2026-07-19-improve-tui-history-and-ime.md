# Task Tree

- [done] 改进TUI历史浏览与输入法定位
  - [done] 调查Mosaic的历史滚动输入与终端光标能力
  - [done] 为历史区域增加可控滚动视口和回归测试
  - [done] 让输入框维护逻辑光标、由屏幕层维护焦点
  - [done] 将终端物理光标定位到输入行以锚定输入法候选框
  - [done] 在Linux原生TUI路径验证滚动与输入法光标输出

# Details

当前历史渲染始终截取尾部，没有浏览此前内容的状态；同时普通`Text`渲染没有将终端光标放在输入行，导致输入法候选框与光标停留在终端左下角。

历史区域通过`PageUp`、`PageDown`、`Home`与`End`维护滚动偏移。输入框维护Unicode标量级逻辑光标并按终端单元宽度计算可视片段；外层维护焦点并只将物理终端光标交给当前焦点组件。Linux PTY验证了`你`后的ANSI定位为第4列。
