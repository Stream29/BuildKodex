# Task Tree

- 支持从任意历史消息执行回退或分叉
  - 明确历史消息到回退与分叉边界的映射
  - 调查历史消息右键事件与悬浮菜单的接入点
  - 确定 `revert here` 与 `fork here` 的会话状态及界面行为
  - 形成实现与验证方案

# Details

- 用户可以对任意一条历史消息点击右键。
- 右键后显示悬浮菜单。
- 菜单提供 `revert here` 与 `fork here`。
- 本次仅登记任务；任务保持 discussion 状态，尚未开始调查、规划或实现。
