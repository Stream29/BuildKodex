# Task Tree

- 规划基于目标路径的项目上下文激活
  - 盘点工具调用在执行副作用前能够提供的目标路径信息
  - 定义跨嵌套仓库的 `AGENTS.md` 发现、作用域与优先级
  - 定义 skills 的路径作用域、发现与延迟加载语义
  - 明确多目标路径、任意 shell 命令、缓存与持久化边界
  - 形成经用户审核的实施顺序与后续任务拆分

# Details

- 状态：`await planning`。
- 等待用户未来明确启动；当前不进行进一步调研、设计或实现。
- Rust Codex 只自动注入 project root 到当前 `cwd` 之间的 `AGENTS.md`，内层目录的指令依赖模型自行发现。
- 目标方向是根据即将访问或修改的路径解析适用上下文，不把嵌套 Git 仓库根目录视为硬边界。
- 外层工作区规则与目标内层目录规则应同时生效，越深层的规则只覆盖其目录子树。
- 上下文必须在相关副作用执行前生效；AGENTS.md 与 skills 是否共享完全相同的激活和持久化机制仍需在规划阶段确定。
