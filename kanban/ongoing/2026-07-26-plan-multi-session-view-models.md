# Task Tree

- 支持 CLI 多 Session、按需 Agent ViewModel 与状态投影优化
  - [done] 收集现有状态与执行所有权信息
    - [done] 盘点 `SessionManager` 的 Session registry、selected-only 投影和后台执行路径
    - [done] 盘点 `CodexCliViewModel` 的状态、命令队列、草稿和 dialog 生命周期
    - [done] 盘点 Mosaic frontend-local history 状态与既有 NewSession 决策
    - [done] 盘点 aggregate StateFlow、完整 history snapshot 和 LazyColumn 数据路径
  - [done] 确定目标边界
    - [done] 定义 application-global、per-session、lazy per-agent、NewSession 和 frontend-local 五个 scope
    - [done] 固定 Session ViewModel 包含 Agent ViewModel tree
    - [done] 固定 root Agent ViewModel 常驻、subagent ViewModel 按需 materialize
    - [done] 定义按一致性边界拆分 state slice 的规则
    - [done] 定义 tail-first、cursor-based lazy history contract
    - [done] 固定复用 AgentStorage index cache 与 raw-value LRU，不在 ViewModel 重建 raw cache
    - [done] 定义 Compose stability、strong skipping、stable key、content type 和窄重组边界
    - [done] 固定 revert 采用整窗generation失效与有限窗口重载，不实现suffix级语义失效
    - [done] 保持 runtime/response 执行逻辑不变，只调整显式寻址和状态投影
    - [done] 记录 canonical ViewModel contract
  - 拆分 Session manager 投影
    - 建立轻量 Session catalog flow
    - 从 coordinator snapshot 建立轻量 Agent topology flow
    - 为每个 Agent 提供按需详细投影入口或稳定 handle
    - 为每个 Agent 发布有序HistoryAppended与HistoryWindowInvalidated投影事件
    - 将 selected-only API 改为显式 Session/Agent target 或 handle
    - 保留旧单选投影作为迁移期 adapter，再在调用方迁移完成后删除
  - 建立 application-global ViewModel
    - 管理 opened Session ViewModel registry
    - 管理 active Session/NewSession target 和全局 overlay
    - 管理 models、global settings、Session browser 和 Session 生命周期命令
    - 保留跨 Session 的单一命令串行化与 shutdown flush 边界
    - 抽出唯一 NewSession ViewModel 并保持 defaults authority 不变
  - 建立 per-session ViewModel
    - 持有 Session lifecycle handle、root Agent ViewModel 和 materialized Agent registry
    - 持有轻量 Agent topology、selected Agent、root title 和 aggregate running
    - 按 Agent address 创建并复用 Agent ViewModel
    - 隔离 Session selection、通知和关闭/flush 生命周期
  - 建立递归 per-agent ViewModel
    - 让 root Agent ViewModel 随 Session ViewModel 常驻
    - 让 subagent ViewModel 只在选择、展开或显式打开时 materialize
    - 用 parent address、direct-child slots 和共享 registry 表达树关系
    - 隔离每个 Agent 的 composer、revision、配置草稿、通知和 pending checkout
    - 隔离 request-user-input draft 与 auto-resolution job
    - 将详细 settings、history source和stream投影限制在已 materialize Agent
    - 使用显式 Agent address 路由 submit、cancel、rename、settings 和 input answer
    - 由 Session ViewModel 在 close 时清理已 materialize Agent ViewModel tree，不接管 runtime resource ownership
  - 拆分 ViewModel state
    - 将 application catalog、navigation、overlay 和 lifecycle 拆成独立 slices
    - 将 Session summary、topology、selection 和 lifecycle/notification 拆成独立 slices
    - 将 Agent configuration、composer、execution、stream 和 blocking interaction 拆成独立 slices
    - 直接复用 global settings 与 model catalog Flow，不保存重复 aggregate snapshot
    - 保留跨字段原子 invariant，避免退化为一个字段一个 Flow
  - 建立 lazy history source
    - 定义基于stored index、exclusive boundary与window generation的cursor/window模型
    - 在既有CachedAgentStorage之上建立薄source，不复制index list或raw value/page cache
    - 捕获稳定tail upper bound并循环prevIndex/get加载有限tail与更早batch
    - 将 committed history、stream tail 和activity分开发布
    - 处理跨batch tool call/output配对与storage-derived stable identity
    - 定义不原地修改的HistoryWindowSnapshot并在普通更新中复用未变entry实例
    - 将append、stream commit与tool output实现为同generation的局部window更新
    - 将checkout/revert实现为generation递增、旧请求取消与当前有限window重载
    - 保持CachedIndexVersioned revert整体invalidate raw-value LRU并让窗口重载重新填热cache
    - 让semantic window保持有界并按两端cursor回收远离viewport的batch
    - 将completed-turn与checkout point改为按需查询或增量索引
  - 迁移 Mosaic frontend
    - 从 application ViewModel 解析 active NewSession 或 Session ViewModel
    - 从 Session ViewModel 解析 selected Agent ViewModel
    - 使用轻量 topology 渲染未 materialize Agent 节点
    - 让各组件只collect实际消费的最窄state slice
    - 为application、Session、Agent、history list和row建立可跳过Composable边界
    - 为UI state与entry提供真实的Stable/Immutable契约并保留未变对象identity
    - 让history list只collect有限window snapshot，让每个row接收immutable entry与自身展开状态
    - 让LazyColumn消费有限semantic window并用layoutInfo驱动前边界预取
    - 用snapshotFlow加distinctUntilChanged去重加载触发，用derivedStateOf收敛阈值状态
    - 自适应填满viewport与overscan，禁止在composition或measure中读取storage
    - 为history item提供stable key与contentType，并按item隔离展开状态
    - 使用generation-scoped item key并在revert时整体清理旧item presentation state
    - 记忆纯展示格式化结果和item provider，复用并按基准调优subcomposition slots
    - 让 overlay 保存显式 Session 或 Agent owner identity
    - 保持 Agent tree expansion、history viewport、follow-tail、focus 和 popup anchor 为 frontend-local
    - 保持当前单终端布局，不在本任务设计 tabs 或 split panes
  - 补充验证
    - 覆盖 Session open 创建 Session ViewModel 且只 materialize root Agent
    - 覆盖多个 Session ViewModel 的 selected Agent、registry 和通知隔离
    - 覆盖展开 direct children、选择深层 Agent 和已 materialize ViewModel 复用
    - 覆盖多个 Agent 的 composer、draft、notification、request-user-input 和 auto-resolution 隔离
    - 覆盖未 materialize 后台 Agent 的轻量状态与打开后的完整状态衔接
    - 覆盖 close 清理、NewSession materialization 和 shutdown flush
    - 覆盖全局 settings/models 向所有已 materialize Agent ViewModel 的单向传播
    - 覆盖多个 frontend 共享 Session/Agent ViewModel、独立保存 tree/history presentation state
    - 覆盖长history有限prevIndex/get、无重复raw cache、prepend锚点和跨batch tool pairing
    - 覆盖viewport预取、自适应首屏填充、请求去重与Agent切换取消
    - 覆盖revert整窗generation失效且只重载viewport、overscan与最小semantic boundary
    - 覆盖revert整体清空raw-value LRU、cold window reload有界且随后重新命中cache
    - 覆盖revert不继承旧item state，surviving anchor恢复且deleted anchor回退tail
    - 覆盖composer/stream高频更新不发布无关state slices
    - 使用Compose compiler report和重组计数覆盖未变row跳过、tail局部更新与单row展开
    - 记录超长history滚动的storage read、projection、composition、recomposition和slot reuse计数
    - 运行 CLI app 相关 Gradle 测试与适用的平台链接验证

# Details

- 状态：规划完成，尚未开始实现；后续节点不构成自动实施授权。
- ViewModel层级边界见 [`checklist/cli-session-view-models.md`](../../checklist/cli-session-view-models.md)，state切分与lazy history见 [`checklist/cli-view-model-state.md`](../../checklist/cli-view-model-state.md)。
- 当前工作树和 IntelliJ IDEA 中存在大量用户未提交改动；本规划以 2026-07-26 的工作树为基线，不改动这些实现。
- `SessionManager` 已用 `sessions: MutableMap<Int, ManagedSession>` 和独立 Session scope 同时持有多个运行实例；切换 target 不会关闭旧实例：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:220`、`:347`、`:1079`。
- 当前瓶颈位于投影和 UI ownership：`SessionManagerState` 只发布一个 `activeSession`，`publishLocked` 只投影 selected Session/Agent：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:113`、`:1434`。
- `MultiAgentSnapshot` 已提供 Agent identity、parent、task name、status 和 activity version，可作为不含完整 history 的轻量拓扑：`CodexLite/agent-runtime/multi-agent/src/commonMain/kotlin/io/github/stream29/codex/lite/agentruntime/multiagent/MultiAgentCoordinatorModels.kt:87`。
- 现有 coordinator 会递归恢复执行节点和 runtime；这是执行层现状，不要求 UI 同时 materialize 对应 Agent ViewModel：`CodexLite/agent-runtime/multi-agent/src/commonMain/kotlin/io/github/stream29/codex/lite/agentruntime/multiagent/MultiAgentCoordinator.kt:301`、`:311`、`:334`。
- 当前 CLI 在每次创建 `ManagedAgent` 时立即调用 `snapshot()`，而该方法会扫描完整 storage history；因此即使 UI 未打开 subagent，也已经构造详细 conversation projection：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:1192`、`:1357`。
- `ManagedAgent` 继续作为执行资源 owner；Agent ViewModel 是可选的 UI projection，两者不得再要求一一同时创建：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:1679`。
- `ManagedSession.treeEntries()` 已按 `parentAgentId` 组织 coordinator snapshots；树结构可与详细 Agent ViewModel 分离：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:1564`。
- Session ViewModel 是一个已打开 Session 的 UI ownership 单位：它承接 `ManagedSession` 当前混合的 selected Agent、topology 和 root summary 投影，但不接管 coordinator/runtime 资源。
- 当前 `CodexCliUiState` 将全局设置、模型目录、Session target、selected Session snapshot、composer、overlay、request-user-input 和退出状态放在一个对象中：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/CodexCliViewModel.kt:42`。
- Mosaic 顶层一次性collect完整`CodexCliUiState`，因此任一slice变化都会从screen根部进入同一重组路径：`CodexLite/cli/app/src/mosaicMain/kotlin/io/github/stream29/codex/lite/cli/app/MosaicView.kt:81`。
- 当前单一 command channel 和 auto-resolution job 也属于全局 ViewModel：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/CodexCliViewModel.kt:357`。命令串行化应保留在 application scope，auto-resolution 必须下沉到对应 Agent scope。
- 当前 composer 和 Agent settings draft 会随 active target 复用；per-agent ViewModel 必须阻止切换后把输入或异步 completion 应用到另一 Agent：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/CodexCliViewModel.kt:594`、`:1348`。
- `SessionSnapshot` 直接包含完整`conversationHistory`，`ManagedAgent.snapshot()`每次从头扫描storage history并全量投影：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:165`、`:179`、`:1357`、`:1364`、`:1401`。
- `HistoryView` 在进入`LazyColumn`前仍将完整conversation list转换为`HistoryEntry`；当前只有composition和换行是lazy，数据读取与语义投影不是lazy：`CodexLite/cli/app/src/mosaicMain/kotlin/io/github/stream29/codex/lite/cli/app/HistoryView.kt:51`、`:66`、`:112`。
- AgentStorage已有`prevIndex`、`indexesDescending(from)`和stored-index读取，可直接支撑首版向前遍历；不需要先修改storage contract：`CodexLite/agent-storage/contract/src/commonMain/kotlin/io/github/stream29/codex/lite/agentstorage/contract/IndexVersioning.kt:66`、`:89`。
- `CachedAgentStorage`在AgentSession打开时一次读取stored indexes并持有内存index cache，`get`使用有界cache4k LRU读取精确stored value；append/revert已负责同步维护两类cache：`CodexLite/agent-session/filesystem/src/commonMain/kotlin/io/github/stream29/codex/lite/agentsession/filesystem/CachedAgentStorage.kt:80`、`:98`、`:101`、`:108`、`:120`、`:136`、`:157`。
- 当前filesystem cache在revert时裁剪stored-index cache并执行`values.invalidateAll()`；完整value-cache失效是破坏性换代的正确语义，目标ViewModel应在新generation中冷重载当前有限窗口：`CodexLite/agent-session/filesystem/src/commonMain/kotlin/io/github/stream29/codex/lite/agentsession/filesystem/CachedAgentStorage.kt:157`、`:165`、`:166`。
- FileSystem Session repository默认给每条timeline配置256项value LRU；history window item budget应与该容量及viewport/overscan协同验证，使cold reload只读取窗口并立即重新填热cache：`CodexLite/agent-session/filesystem/src/commonMain/kotlin/io/github/stream29/codex/lite/agentsession/filesystem/FileSystemCodexSessionRepository.kt:33`。
- 因此前台history I/O主循环可以保持为稳定upper bound上的`get(index)`与`prevIndex(index)`；Agent ViewModel只需要管理有限semantic window与generation，不需要重新设计storage分页或局部revert协议。
- 现有conversation projector通过完整列表配对tool call/output；增量实现必须显式处理跨batch关联并用storage index替代batch-relative ordinal作为fallback identity：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/ConversationHistory.kt:92`。
- 当前`LazyListState`已经发布visible item、viewport和total item信息，足以作为异步预取信号：`CodexLite/cli/components/src/mosaicMain/kotlin/io/github/stream29/codex/lite/cli/components/LazyListState.kt:20`、`:73`。
- 当前`LazyColumn`已有remembered `SubcomposeLayoutState`，并支持stable key和`contentType`，但每次执行函数体仍重建item provider与完整key index，slot reuse固定为2；迁移时应结合窄重组边界、provider记忆和基准调优：`CodexLite/cli/components/src/mosaicMain/kotlin/io/github/stream29/codex/lite/cli/components/LazyColumn.kt:32`、`:33`，`CodexLite/cli/components/src/mosaicMain/kotlin/io/github/stream29/codex/lite/cli/components/LazyListScope.kt:47`、`:121`。
- 工程使用Kotlin 2.4 Compose compiler plugin，strong skipping默认可用；稳定参数仍按value equality、unstable参数按instance equality比较，因此保留未变window、entry和callback identity仍是关键：`CodexLite/gradle/libs.versions.toml:2`、`:81`。
- 当前checkout在storage revert成功后调用`refreshLocked(agent)`，后者重新执行完整snapshot；目标投影层应改为提升window generation并只重载当前有限window，AgentState的revert与runtime rebuild语义保持不变：`CodexLite/cli/app/src/commonMain/kotlin/io/github/stream29/codex/lite/cli/app/model/SessionManager.kt:501`、`:509`、`:510`。
- Mosaic 已通过 `HistoryUiStateStore` 按 Agent storage id 保存独立 history presentation state，这部分边界保持不变：`CodexLite/cli/app/src/mosaicMain/kotlin/io/github/stream29/codex/lite/cli/app/HistoryUiState.kt:118`。
- 现有 NewSession contract 明确它不是持久化 Session，并以 `CodexGlobalSettings.newSession` 为唯一 defaults 真源；本任务只把这套状态从 global 大对象抽成唯一 child ViewModel，不改变物化语义。
- 本任务不修改 AgentRuntime 装饰链、Responses 调用、tool/hook 执行、storage schema、repository lease 或 multi-agent coordinator 的执行语义；coordinator 可以继续恢复执行节点，UI 不再为每个节点预先构造 Agent ViewModel。
- 本任务不决定多 Session 的最终视觉形态；tabs、split panes 和多窗口属于建立稳定 ViewModel registry 后的独立 UI 任务。
